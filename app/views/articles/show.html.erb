<div class="wall-image">
  <h2>タイトル</h2>
  <p><%= @article.title %></p>

  <h2>本文</h2>
  <p><%= @article.body %></p>

  <%= link_to "編集", article_path(@article.id) + '/edit', class: "btn edit-btn" %>
  <%= link_to "削除", article_path(@article.id), method: :delete, class: "btn destroy-btn", "data-confirm" => "本当に削除しますか" %> 
</div>
